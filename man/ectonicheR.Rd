% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ectonicherR.R
\name{ectonicheR}
\alias{ectonicheR}
\title{Microclimate-based physiological niche modeling}
\usage{
ectonicheR(
  rcover = NULL,
  rtop = NULL,
  rast_or_coord = NULL,
  traits_df = NULL,
  method_cover = "near",
  method_top = "bilinear",
  use_disk = FALSE,
  resample_res = NULL,
  sample_n = NULL,
  sample_frac = NULL,
  seed = NULL,
  maxshade = 100,
  runshade = 1,
  Usrhyt = 0.01,
  Ww_g = 40,
  pct_wet = 0.2,
  alpha_min = 0.85,
  alpha_max = 0.85,
  shape = 3,
  T_RB_min = 17.5,
  T_B_min = 17.5,
  T_F_min = 24,
  T_F_max = 34,
  T_pref = 30,
  CT_max = 40,
  CT_min = 6,
  mindepth = 2,
  maxdepth = 10,
  shade_seek = 1,
  burrow = 1,
  climb = 0,
  nocturn = 0,
  crepus = 0,
  diurn = 1,
  list_format = TRUE,
  summary = FALSE,
  break_n = NULL,
  checkpoint_dir = NULL,
  resume = TRUE
)
}
\arguments{
\item{rcover}{SpatRaster. Raster containing vegetation cover or shade values.}

\item{rtop}{SpatRaster. Raster containing elevation or topographic values.}

\item{rast_or_coord}{SpatRaster, matrix, or data.frame. Study raster or coordinates used to generate the environmental input data frame.}

\item{traits_df}{data.frame or NULL. Table containing species-level physiological, morphological, and behavioural parameters.}

\item{method_cover}{Character. Resampling or extraction method for the cover raster passed to \code{rast_to_df()}. Default is \code{"near"}.}

\item{method_top}{Character. Resampling or extraction method for the elevation raster passed to \code{rast_to_df()}. Default is \code{"bilinear"}.}

\item{use_disk}{Logical. If TRUE, intermediate raster operations are written to disk using temporary files. Default is FALSE.}

\item{resample_res}{Numeric or NULL. Target resolution used by \code{rast_to_df()} when coordinates are supplied.}

\item{sample_n}{Integer or NULL. Optional number of pixels or locations to sample before running the microclimate model.}

\item{sample_frac}{Numeric or NULL. Optional fraction of pixels or locations to sample before running the microclimate model.}

\item{seed}{Integer or NULL. Optional random seed used when \code{sample_n} or \code{sample_frac} is supplied.}

\item{maxshade}{Numeric. Maximum shade value passed to \code{NicheMapR::micro_global()}. Default is 100.}

\item{runshade}{Numeric. Shade mode passed to \code{NicheMapR::micro_global()}. Default is 1.}

\item{Usrhyt}{Numeric. User-specified reference height passed to \code{NicheMapR::micro_global()}. Default is 0.01.}

\item{Ww_g}{Numeric. Wet body mass (g).}

\item{pct_wet}{Numeric. Fraction of body surface acting as a free-water exchanger.}

\item{alpha_min}{Numeric. Minimum solar absorptivity.}

\item{alpha_max}{Numeric. Maximum solar absorptivity.}

\item{shape}{Numeric. Body shape code used by \code{NicheMapR::ectotherm()}.}

\item{T_RB_min}{Numeric. Minimum body temperature at which the animal attempts to leave its retreat.}

\item{T_B_min}{Numeric. Minimum body temperature required for basking.}

\item{T_F_min}{Numeric. Minimum body temperature allowing activity.}

\item{T_F_max}{Numeric. Maximum body temperature allowing activity.}

\item{T_pref}{Numeric. Preferred body temperature.}

\item{CT_max}{Numeric. Critical thermal maximum.}

\item{CT_min}{Numeric. Critical thermal minimum.}

\item{mindepth}{Numeric. Minimum burrow depth (soil node).}

\item{maxdepth}{Numeric. Maximum burrow depth (soil node).}

\item{shade_seek}{Numeric. Indicates whether shade seeking behaviour is enabled.}

\item{burrow}{Numeric. Indicates whether burrowing behaviour is enabled.}

\item{climb}{Numeric. Indicates whether climbing behaviour is enabled.}

\item{nocturn}{Numeric. Indicates nocturnal activity.}

\item{crepus}{Numeric. Indicates crepuscular activity.}

\item{diurn}{Numeric. Indicates diurnal activity.}

\item{list_format}{Logical. If TRUE, returns a nested list by species, energy balance (enbal), evaporative water loss (masbal), and pixel. If FALSE, returns a tidy data frame.}

\item{summary}{Logical. If TRUE and \code{list_format = FALSE}, returns daily summary statistics by species and pixel.}

\item{break_n}{Numeric or NULL. Optional chunk size used to split large pixel/location data frames into smaller blocks before running the microclimate and physiological models. This option is useful for reducing memory usage during large simulations.}

\item{checkpoint_dir}{Character. Directory used to store checkpoint files during block processing. If NULL (default), checkpoint files are not created.}

\item{resume}{Logical. If TRUE and checkpoint files already exist, previously completed blocks are loaded and skipped. Default is TRUE.}
}
\value{
If \code{list_format = TRUE}, returns a nested list with one element per species, each containing \code{energy} (\code{enbal}) and \code{evap} (\code{masbal}) lists for every pixel.

If \code{list_format = FALSE}, returns a tidy data frame containing species, pixel coordinates, day, time, air temperature, energy balance (\code{enbal}), and evaporative water loss (\code{masbal}).

If \code{summary = TRUE}, returns daily summary statistics for each species and pixel.
}
\description{
Runs microclimate and ectotherm simulations for one or more species. The function first converts raster or coordinate inputs into a pixel-level environmental data frame using \code{rast_to_df()}, then runs \code{NicheMapR::micro_global()} once per pixel and reuses the resulting microclimate for all species in \code{traits_df}.
}
\details{
Run microclimate-based ectotherm physiological niche simulations
}
\examples{
\dontrun{
out <- micronicheR::ectonicheR(
    rcover = cover_raster
  , rtop = elevation_raster
  , rast_or_coord = study_raster
  , traits_df = traits_table
  , list_format = FALSE
  , summary = TRUE
)
}
}
\author{
\itemize{
\item Zárate-Salazar, J. Rafael, PhD
\item Valença, Saulo E. S., MSc
\item Castro, Luis Miguel Senzano, PhD
\item Rubalcaba, Juan G., PhD
\item Gouveia, Sidney Feitosa, PhD
}
}
